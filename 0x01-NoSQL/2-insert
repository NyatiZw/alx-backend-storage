// Script that inserts a document in a collection
#!/usr/bin/bash

document='{
  "name": "Holberton school"
}'

mongo --eval "db.school.insert($document)"

echo "WriteResult({ "$document" : 1 })"

echo "bye"
